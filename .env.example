DATABASE_URL=postgresql://loom:loom_dev_password@localhost:5432/loom
PORT=3000
HOST=0.0.0.0
OPENAI_API_KEY=your_openai_api_key_here
# Required for trace encryption. Any 32+ character string works for local dev.
ENCRYPTION_MASTER_KEY=<64-hex-chars — generate with: node -e "require('crypto').randomBytes(32).toString('hex')" | tee out>
